const apiUrl = `${process.env.RAZZLE_API_HOST}:${process.env.RAZZLE_API_PORT}`;

export const PAINTING_URL = `${apiUrl}/paintings`;
export const SCULPTURE_URL = `${apiUrl}/sculptures`;
export const DRAWING_URL = `${apiUrl}/drawings`;
export const CONTENT_URL = `${apiUrl}/contents`;
export const USER_URL = `${apiUrl}/users`;
